# Integration

## Framework Integration

- **SolidStart** - SolidStart framework สำหรับ SSR
- **Vite** - Vite plugin สำหรับ development
- **TypeScript** - TypeScript support ครบถ้วน

## Integration Patterns

```typescript
import { createSignal } from 'solid-js';

// React-like patterns
const [count, setCount] = createSignal(0);
```
