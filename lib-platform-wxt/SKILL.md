---
name: wxt
description: Framework for building web extensions with TypeScript and auto-imports
goal: สร้าง web extensions ด้วย WXT ตาม best practices
outcome: Web extensions มีโครงสร้างและคุณภาพตามมาตรฐาน
---

# WXT

## Rules

| Priority | Impact | Reference | Name | Description | Prefix | Condition |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `CRITICAL` | `1-wxt-project-structure` | Project Structure | โครงสร้างโปรเจกต์ WXT ที่ถูกต้อง | `wxt-` | เมื่อสร้างโปรเจกต์ WXT ใหม่ |
| 2 | `HIGH` | `2-wxt-entrypoints` | Entrypoints | การใช้งาน entrypoints ใน WXT | `wxt-` | เมื่อสร้าง entrypoints ใหม่ |
| 3 | `HIGH` | `3-wxt-manifest` | Manifest Configuration | การกำหนดค่า manifest ใน WXT | `wxt-` | เมื่อกำหนดค่า manifest |
| 4 | `MEDIUM` | `4-wxt-content-scripts` | Content Scripts | การใช้งาน content scripts | `wxt-` | เมื่อสร้าง content scripts |
| 5 | `MEDIUM` | `5-wxt-background` | Background Scripts | การใช้งาน background scripts | `wxt-` | เมื่อสร้าง background scripts |
| 6 | `MEDIUM` | `6-wxt-auto-imports` | Auto-imports | การใช้งาน auto-imports | `wxt-` | เมื่อต้องการใช้ auto-imports |
| 7 | `LOW` | `7-wxt-modules` | Modules | การใช้งาน WXT modules | `wxt-` | เมื่อใช้ modules |
| 8 | `LOW` | `8-wxt-hooks` | Hooks | การใช้งาน hooks | `wxt-` | เมื่อใช้ hooks |

## Scripts

| Priority | Impact | Reference | Name | Description | Prefix | Condition |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | `CRITICAL` | `1-wxt-init` | Init Project | เริ่มต้นโปรเจกต์ WXT ใหม่ | `wxt-` | เมื่อสร้างโปรเจกต์ใหม่ |
| 2 | `HIGH` | `2-wxt-dev` | Dev Mode | เริ่มโหมด development | `wxt-` | เมื่อต้องการพัฒนา |
| 3 | `HIGH` | `3-wxt-build` | Build Extension | Build extension สำหรับ production | `wxt-` | เมื่อต้องการ build |
| 4 | `MEDIUM` | `4-wxt-zip` | Zip Extension | Zip extension สำหรับ publishing | `wxt-` | เมื่อต้องการ zip |
| 5 | `MEDIUM` | `5-wxt-publish` | Publish Extension | Publish extension ไปยัง stores | `wxt-` | เมื่อต้องการ publish |

## Overview

WXT เป็น framework สำหรับสร้าง web extensions ที่รองรับหลาย browser (Chrome, Firefox, Edge, Safari) และทั้ง MV2 และ MV3 มีคุณสมบัติหลักๆ ดังนี้:

- **Multi-browser Support**: รองรับ Chrome, Firefox, Edge, Safari และ Chromium browsers
- **MV2 & MV3**: สร้าง extensions สำหรับทั้ง Manifest V2 และ V3
- **Fast HMR**: Hot Module Replacement ที่รวดเร็วสำหรับ UI development
- **File-based Entrypoints**: Manifest ถูกสร้างจากไฟล์ในโปรเจกต์
- **TypeScript**: รองรับ TypeScript โดย default
- **Auto-imports**: Nuxt-like auto-imports เพื่อเร่งการพัฒนา
- **Frontend Framework Agnostic**: รองรับหลาย frontend frameworks
- **Module System**: Reuse code ได้หลาย extensions
- **Automated Publishing**: Zip, upload, submit, และ publish extensions อัตโนมัติ
- **Bundle Analysis**: Tools สำหรับวิเคราะห์ bundle size

## Project Structure

โครงสร้างโปรเจกต์ WXT มีดังนี้:

```text
📂 {rootDir}/
├── .output/           # Build artifacts
├── .wxt/              # Generated by WXT (TS config)
├── assets/            # CSS, images, assets
├── components/        # UI components (auto-imported)
├── composables/      # Composable functions (auto-imported)
├── entrypoints/      # Entrypoints ทั้งหมด
├── hooks/            # React/Solid hooks (auto-imported)
├── modules/          # WXT modules
├── public/           # Static files
├── utils/            # Utility functions (auto-imported)
├── .env              # Environment variables
├── .env.publish      # Environment variables for publishing
├── app.config.ts     # Runtime config
├── package.json      # Package manager file
├── tsconfig.json     # TypeScript config
├── web-ext.config.ts # Browser startup config
└── wxt.config.ts     # Main WXT config
```

## Entrypoints

WXT ใช้ไฟล์ใน `entrypoints/` เป็น inputs เมื่อ bundle extension แต่ละ entrypoint ถูกกำหนดจากชื่อไฟล์หรือโฟลเดอร์:

- `background.ts` หรือ `background/index.ts` - Background script
- `popup.html` หรือ `popup/index.html` - Popup page
- `options.html` หรือ `options/index.html` - Options page
- `content.ts` - Content script (ใช้ defineContentScript)
- และอื่นๆ

## Manifest Generation

Manifest ถูกสร้างจากหลายแหล่ง:

1. Global options ใน `wxt.config.ts`
2. Entrypoint-specific options ในแต่ละ entrypoint
3. WXT Modules
4. Hooks

## Installation

ใช้คำสั่ง init เพื่อสร้างโปรเจกต์ใหม่:

```bash
pnpm dlx wxt@latest init
# หรือ
bunx wxt@latest init
# หรือ
npx wxt@latest init
```

## Development

เริ่มโหมด development:

```bash
wxt
```

Browser จะเปิดอัตโนมัติเมื่อเริ่ม development

## Building

Build extension สำหรับ production:

```bash
wxt build
```

Target browser เฉพาะ:

```bash
wxt -b chrome
wxt -b firefox
```

## Publishing

Publish extension ไปยัง stores:

```bash
wxt publish
```

## References

- [WXT Documentation](https://wxt.dev/)
- [WXT GitHub](https://github.com/wxt-dev/wxt)
