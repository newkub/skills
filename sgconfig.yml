# Ast-grep configuration for skills repository
# This config defines rule directories for different categories

ruleDirs:
  - rules/skill-structure
  - rules/markdown-quality
  - rules/naming-conventions

testDirs:
  - rules/__tests__

ignore:
  - node_modules
  - .git
  - dist
  - build
