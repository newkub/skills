# Configuration

การตั้งค่า RMUX

## Config File

RMUX ใช้ config file ที่ `~/.rmux/config.toml`

## Example Config

```toml
[session]
default_shell = "zsh"
default_path = "~/projects"

[ui]
status_bar = true
mouse_support = true
```
